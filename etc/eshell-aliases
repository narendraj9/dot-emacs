alias with-jvm $SHELL -c "${cs java --env --jvm $1} ; $@*[1..]"
alias cdp cd (project-root (project-current))
alias send-to-tab bluetooth-sendto --device ${(format "%s" tablet-mac-address)} $*
alias nu nu -c $@*
alias sg src search -less=0 -- "context:global $@*"
alias clear clear-scrollback
